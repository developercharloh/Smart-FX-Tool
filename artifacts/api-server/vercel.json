{
  "version": 2,
  "installCommand": "cd ../../ && pnpm install --frozen-lockfile && pnpm --filter @workspace/db run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "functions": {
    "api/index.ts": { "maxDuration": 60, "memory": 512 }
  }
}
